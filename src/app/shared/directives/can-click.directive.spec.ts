import { CanClickDirective } from './can-click.directive';

describe('CanClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CanClickDirective();
    expect(directive).toBeTruthy();
  });
});
